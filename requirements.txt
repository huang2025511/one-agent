# ============================================================
# One-Agent 依赖
# ============================================================
# 国内网络慢？设置镜像后重试：
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
# ============================================================

# ── 核心依赖（必须）──────────────────────────────────────────
httpx>=0.27.0,<1.0
pyyaml>=6.0.1,<7.0
pydantic>=2.5.0,<3.0

# ── Web UI / REST API（可选）─────────────────────────────────
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0

# ── 调度器（可选）────────────────────────────────────────────
APScheduler>=3.10.4,<4.0

# ── 加密（可选，推荐）───────────────────────────────────────
cryptography>=41.0.0,<46.0

# ── 二维码生成（微信登录终端显示）───────────────────────────
qrcode>=7.4,<8.0

# ── 个人微信网关（可选）─────────────────────────────────────
# itchat-uos  # 取消注释以启用个人微信网关

# ── 嵌入模型（可选，用于长记忆语义搜索）────────────────────
# 启用语义搜索：内存中 cosine 相似度匹配，未装时降级为 FTS5 关键词搜索。
# 注意：会拉 PyTorch 全栈（CPU 版约 500MB，GPU 版约 2GB）。
# 首次使用时会自动下载模型权重 all-MiniLM-L6-v2（约 80MB）。
sentence-transformers>=5.0.0,<6.0

# ── 测试（开发用）────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23.0,<1.0
pytest-cov>=4.0,<7.0
# 修复 #5：CI 用 --timeout=120/--timeout=180 控制单测耗时，需声明为依赖
pytest-timeout>=0.13.0