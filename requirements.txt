# ============================================================
# One-Agent 依赖
# ============================================================
# 国内网络慢？设置镜像后重试：
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
# ============================================================

# ── 核心依赖（必须）──────────────────────────────────────────
httpx>=0.27.0
pyyaml>=6.0.1
pydantic>=2.5.0

# ── Web UI / REST API（可选）─────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ── 调度器（可选）────────────────────────────────────────────
APScheduler>=3.10.4

# ── 加密（可选，推荐）───────────────────────────────────────
cryptography>=41.0.0

# ── 个人微信网关（可选）─────────────────────────────────────
# itchat-uos  # 取消注释以启用个人微信网关

# ── 嵌入模型（可选，用于长记忆语义搜索）────────────────────
# sentence-transformers  # 取消注释以启用语义搜索（包较大，约 2GB）

# ── 测试（开发用）────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23.0